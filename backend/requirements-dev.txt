# 开发与测试（pip install -r requirements.txt -r requirements-dev.txt）
# 建议使用 Python 3.10+（与主依赖 opendataloader-pdf 等一致）；可创建 backend/.venv-test 专用测试环境。
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-html>=4.1.0
httpx>=0.27.0
